To run the app and seed the database with initial growth trait data, follow these steps:

1. **Navigate to the `vite` directory**:
   ```sh
   cd vite
   ```

2. **Run the app**:
   ```sh
   yarn start
   ```

3. **Seed the database with initial growth trait data**:
   ```sh
   node src/seedData.js
   ```